{
  "name": "nova-marketplace",
  "private": true,
  "type": "module",
  "description": "Buyer app for Nova Marketplace. The site itself has no build step and no runtime dependencies; everything here is tooling.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node devserver/server.mjs",
    "seed": "node scripts/seed.mjs",
    "test": "node --test scripts/money.test.mjs",
    "deploy": "npx wrangler deploy",
    "test:db": "node --test scripts/db.test.mjs",
    "icons": "python3 scripts/make-icons.py",
    "check": "npm run lint && npm test && npm run test:db",
    "lint": "node scripts/lint-css.mjs",
    "seo": "node scripts/build-seo.mjs"
  }
}
